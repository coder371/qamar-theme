{# Related Products Widget - Qamar Theme #}
{% set related_products = context.related_products %}
{% set layout = widget.data.layout | default('slider') %}
{% set columns_desktop = widget.data.columns_desktop | default('4') %}
{% set autoplay = widget.data.autoplay | default(false) %}
{% set autoplay_speed = widget.data.autoplay_speed | default('5') %}
{% set show_arrows = widget.data.show_arrows if widget.data.show_arrows is defined else true %}

{# Card display settings (resolved to avoid passing undefined) #}
{% set _cs_rating = widget.data.card_show_rating if widget.data.card_show_rating is defined else true %}
{% set _cs_options = widget.data.card_show_options if widget.data.card_show_options is defined else true %}
{% set _cs_badge = widget.data.card_show_badge if widget.data.card_show_badge is defined else true %}
{% set _cs_button = widget.data.card_show_button if widget.data.card_show_button is defined else true %}
{% set _cs_compare = widget.data.card_show_compare_price if widget.data.card_show_compare_price is defined else true %}
{% set _cs_wishlist = widget.data.card_show_wishlist | default(false) %}
{% set _cs_ratio = widget.data.card_image_ratio | default('square') %}
{% set _cs_lines = widget.data.card_title_lines | default('2') %}
{% set _cs_fit = widget.data.card_image_fit | default('contain') %}
{% set _cs_hover = widget.data.card_hover_effect | default('zoom') %}
{% set _cs_radius = widget.data.card_border_radius | default('lg') %}

{% if related_products | length > 0 %}
<section class="related-products py-10 md:py-14">
  <div class="container mx-auto px-4">

    {# Section Header #}
    {% if widget.data.title %}
      <h2 class="text-lg md:text-xl font-bold text-theme-text mb-6">{{ widget.data.title }}</h2>
    {% endif %}

    {# Grid Layout #}
    {% if layout == 'grid' %}
      <div class="grid grid-cols-2 gap-3 md:gap-5
        {% if columns_desktop == '3' %} md:grid-cols-3
        {% else %} md:grid-cols-4
        {% endif %}">
        {% for product in related_products %}
          {% ui "product-card.njk", product=product, show_rating=_cs_rating, show_options=_cs_options, show_badge=_cs_badge, image_ratio=_cs_ratio, show_button=_cs_button, show_compare_price=_cs_compare, title_lines=_cs_lines, image_fit=_cs_fit, hover_effect=_cs_hover, show_wishlist=_cs_wishlist, border_radius=_cs_radius %}
        {% endfor %}
      </div>

    {# Slider Layout #}
    {% elif layout == 'slider' %}
      <div class="related-swiper-wrap relative">
        <div class="swiper swiper-outside-pagination" x-data x-init="$nextTick(() => {
          new Swiper($el, {
            slidesPerView: 2,
            spaceBetween: 12,
            loop: {{ 'true' if related_products | length > (columns_desktop | int) else 'false' }},
            {% if autoplay %}
            autoplay: {
              delay: {{ autoplay_speed }}000,
              disableOnInteraction: false,
              pauseOnMouseEnter: true
            },
            {% endif %}
            pagination: {
              el: $el.querySelector('.swiper-pagination'),
              clickable: true
            },
            {% if show_arrows %}
            navigation: {
              nextEl: $el.closest('.related-swiper-wrap').querySelector('.rel-btn-next'),
              prevEl: $el.closest('.related-swiper-wrap').querySelector('.rel-btn-prev')
            },
            {% endif %}
            breakpoints: {
              640: { slidesPerView: 2, spaceBetween: 16 },
              768: { slidesPerView: 3, spaceBetween: 20 },
              1024: { slidesPerView: {{ columns_desktop }}, spaceBetween: 24 }
            }
          });
        })">
          <div class="swiper-wrapper">
            {% for product in related_products %}
              <div class="swiper-slide">
                {% ui "product-card.njk", product=product, show_rating=_cs_rating, show_options=_cs_options, show_badge=_cs_badge, image_ratio=_cs_ratio, show_button=_cs_button, show_compare_price=_cs_compare, title_lines=_cs_lines, image_fit=_cs_fit, hover_effect=_cs_hover, show_wishlist=_cs_wishlist, border_radius=_cs_radius %}
              </div>
            {% endfor %}
          </div>
          <div class="swiper-pagination"></div>
        </div>

        {# Navigation Arrows - outside swiper, centered on card images #}
        {% if show_arrows %}
          <button class="rel-btn-prev absolute top-[35%] -translate-y-1/2 -start-3 md:-start-5 z-10
                         w-10 h-10 rounded-full bg-white shadow-md border border-theme-border/40
                         flex items-center justify-center text-theme-text-secondary
                         hover:text-primary hover:shadow-lg transition-all
                         hidden md:flex"
                  aria-label="{{t('common.back')}}">
            <svg class="w-4 h-4 rtl:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M15 19l-7-7 7-7"/>
            </svg>
          </button>
          <button class="rel-btn-next absolute top-[35%] -translate-y-1/2 -end-3 md:-end-5 z-10
                         w-10 h-10 rounded-full bg-white shadow-md border border-theme-border/40
                         flex items-center justify-center text-theme-text-secondary
                         hover:text-primary hover:shadow-lg transition-all
                         hidden md:flex"
                  aria-label="{{t('common.continue')}}">
            <svg class="w-4 h-4 rtl:rotate-180" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2.5" d="M9 5l7 7-7 7"/>
            </svg>
          </button>
        {% endif %}
      </div>
    {% endif %}

  </div>
</section>

{% else %}
  {# Preview / Empty State #}
  <section class="related-products py-10 md:py-14">
    <div class="container mx-auto px-4">
      {# Skeleton Header #}
      <div class="h-6 w-40 bg-surface rounded-full mb-6"></div>

      {# Skeleton Products Grid #}
      <div class="grid grid-cols-2 gap-3 md:gap-5
        {% if columns_desktop == '3' %} md:grid-cols-3
        {% else %} md:grid-cols-4
        {% endif %}">
        {% for i in range(0, columns_desktop | int) %}
          <div class="bg-white rounded-2xl overflow-hidden shadow-sm border border-dashed border-theme-border">
            <div class="aspect-square bg-surface flex items-center justify-center relative">
              <svg class="w-12 h-12 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
              </svg>
            </div>
            <div class="p-3">
              <div class="space-y-2 mb-3">
                <div class="h-3.5 bg-surface rounded-full w-full"></div>
                <div class="h-3.5 bg-surface rounded-full {% if loop.first %}w-3/4{% elif loop.index0 == 1 %}w-2/3{% elif loop.index0 == 2 %}w-4/5{% else %}w-1/2{% endif %}"></div>
              </div>
              <div class="flex items-center gap-0.5 mb-3">
                {% for s in range(5) %}
                  <div class="w-3.5 h-3.5 rounded-sm bg-surface"></div>
                {% endfor %}
              </div>
              <div class="h-4 w-20 bg-surface rounded-full mb-3"></div>
              <div class="h-10 bg-primary/10 rounded-lg"></div>
            </div>
          </div>
        {% endfor %}
      </div>
      <p class="text-center text-sm text-theme-text-secondary/70 mt-6">{{t('product.related_preview_hint')}}</p>
    </div>
  </section>
{% endif %}
