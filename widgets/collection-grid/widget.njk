{% set layout = widget.data.layout | default('circle') %}
{% set columns = widget.data.columns | default('6') %}
{% set collections = widget.data.collections | default([]) %}

<section class="py-12 md:py-16">
  <div class="container mx-auto px-4">

    {# Section Header #}
    {% if widget.data.title or widget.data.subtitle %}
      <div class="text-center mb-10">
        {% if widget.data.title %}
          <h2 class="text-2xl md:text-3xl font-bold text-theme-text mb-3">{{ widget.data.title }}</h2>
        {% endif %}
        {% if widget.data.subtitle %}
          <p class="text-theme-text-secondary text-lg">{{ widget.data.subtitle }}</p>
        {% endif %}
        <div class="w-16 h-1 mx-auto mt-4 rounded-full" style="background-color: var(--color-primary);"></div>
      </div>
    {% endif %}

    {# Collections Grid #}
    {% if collections | length > 0 %}
      <div class="grid grid-cols-2 gap-4 md:gap-6
        {% if columns == '3' %} md:grid-cols-3
        {% elif columns == '4' %} md:grid-cols-4
        {% elif columns == '5' %} md:grid-cols-5
        {% else %} md:grid-cols-6
        {% endif %}">

        {% for collection in collections %}
          <a href="/shop/{{ collection.handle }}" class="group block text-center">

            {% if layout == 'circle' %}
              <div class="aspect-square rounded-full overflow-hidden mx-auto mb-3 w-full max-w-[180px] border-2 border-transparent group-hover:border-secondary transition-all duration-300">
                {% if collection.image %}
                  <img src="{{ collection.image.fileUrl }}" alt="{{ collection.title }}" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" loading="lazy">
                {% else %}
                  <div class="w-full h-full bg-gradient-to-br from-primary/10 to-secondary/10 flex items-center justify-center">
                    <svg class="w-10 h-10 text-primary opacity-40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                    </svg>
                  </div>
                {% endif %}
              </div>
              <h3 class="text-sm md:text-base font-semibold text-theme-text group-hover:text-primary transition-colors duration-300">{{ collection.title }}</h3>

            {% elif layout == 'square' %}
              <div class="aspect-square rounded-xl overflow-hidden relative">
                {% if collection.image %}
                  <img src="{{ collection.image.fileUrl }}" alt="{{ collection.title }}" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" loading="lazy">
                {% else %}
                  <div class="w-full h-full bg-gradient-to-br from-primary/10 to-secondary/10"></div>
                {% endif %}
                <div class="absolute inset-0 bg-black/30 group-hover:bg-black/40 transition-colors duration-300 flex items-center justify-center">
                  <h3 class="text-white text-sm md:text-lg font-bold">{{ collection.title }}</h3>
                </div>
              </div>

            {% elif layout == 'card' %}
              <div class="bg-white rounded-xl overflow-hidden shadow-sm hover:shadow-md transition-shadow duration-300">
                <div class="aspect-[4/3] overflow-hidden">
                  {% if collection.image %}
                    <img src="{{ collection.image.fileUrl }}" alt="{{ collection.title }}" class="w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" loading="lazy">
                  {% else %}
                    <div class="w-full h-full bg-gradient-to-br from-primary/10 to-secondary/10 flex items-center justify-center">
                      <svg class="w-10 h-10 text-primary opacity-40" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1.5" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                      </svg>
                    </div>
                  {% endif %}
                </div>
                <div class="p-3 text-center">
                  <h3 class="text-sm md:text-base font-semibold text-theme-text group-hover:text-primary transition-colors duration-300">{{ collection.title }}</h3>
                </div>
              </div>
            {% endif %}

          </a>
        {% endfor %}

      </div>
    {% else %}
      {# Preview / Empty State #}
      <div class="grid grid-cols-2 gap-4 md:gap-6
        {% if columns == '3' %} md:grid-cols-3
        {% elif columns == '4' %} md:grid-cols-4
        {% elif columns == '5' %} md:grid-cols-5
        {% else %} md:grid-cols-6
        {% endif %}">
        {% for i in range(0, columns | int) %}
          <div class="text-center">
            {% if layout == 'circle' %}
              <div class="aspect-square rounded-full overflow-hidden mx-auto mb-3 w-full max-w-[180px] border-2 border-dashed border-theme-border bg-surface flex items-center justify-center">
                <svg class="w-10 h-10 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
              </div>
              <div class="h-4 w-20 bg-surface rounded-full mx-auto"></div>
            {% elif layout == 'square' %}
              <div class="aspect-square rounded-xl overflow-hidden border border-dashed border-theme-border bg-surface flex items-center justify-center relative">
                <svg class="w-10 h-10 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                </svg>
                <div class="absolute inset-0 bg-black/10 flex items-center justify-center">
                  <div class="h-5 w-24 bg-white/40 rounded-full"></div>
                </div>
              </div>
            {% else %}
              <div class="bg-white rounded-xl overflow-hidden border border-dashed border-theme-border">
                <div class="aspect-[4/3] bg-surface flex items-center justify-center">
                  <svg class="w-10 h-10 text-gray-200" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="1" d="M4 16l4.586-4.586a2 2 0 012.828 0L16 16m-2-2l1.586-1.586a2 2 0 012.828 0L20 14m-6-6h.01M6 20h12a2 2 0 002-2V6a2 2 0 00-2-2H6a2 2 0 00-2 2v12a2 2 0 002 2z"/>
                  </svg>
                </div>
                <div class="p-3">
                  <div class="h-4 w-20 bg-surface rounded-full mx-auto"></div>
                </div>
              </div>
            {% endif %}
          </div>
        {% endfor %}
      </div>
      <p class="text-center text-sm text-theme-text-secondary/70 mt-8">{{t('collection.select_collections_hint')}}</p>
    {% endif %}

  </div>
</section>
